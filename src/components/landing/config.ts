/**
 * Official ALTAR Landing Website Configuration
 * 
 * Update these constants when releasing production download links or official contact endpoints.
 */

// Configure the direct Android APK download URL when a release APK is published.
// Leave as empty string ("") to automatically trigger the "Coming Soon" download modal.
export const ANDROID_DOWNLOAD_URL: string = "";

// Configure the live production Web Application URL.
// Leave as empty string ("") to use the internal application router.
export const WEB_APP_URL: string = "";

// Configure official ministry contact email or link.
// Leave as empty string ("") to display the default contact prompt.
export const CONTACT_EMAIL: string = "";
